The JSON files within this folder are used to render the plugin's menus dynamically. Feel free to customize them.

!!!
Capitilized field values are identifiers. They could be internal IDs (for the plugin) OR Minecraft IDs. You should only really be changing the latter.
If anything breaks, delete the `ui` folder and restart the server. It will be regenerated.
